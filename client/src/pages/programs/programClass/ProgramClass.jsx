/* eslint-disable react/prop-types */
const ProgramClass = ({ program }) => {
    const {
        className,
        description,
        images,
        category,
        price,
        discountPercentage,
        isTrending,
        duration,
        classDates,
        maxStudents,
        students,
        averageRating,
        comments,
    } = program;

    return (
        <div className="">
            <p>Hello</p>
        </div>
    );
};

export default ProgramClass;
